# Kazumi Lite 日用版 0.2.3-r1

## 安装

1. 将 `KazumiLite-Jacaranda-Daily-0.2.3-r1.muxapp` 放入 SD 卡 `ARCHIVE` 文件夹。
2. 在掌机打开“应用”→“归档管理器”，选择安装包。
3. 重新进入应用列表并启动 `Kazumi Lite 番剧`。

安装会覆盖同目录的旧版程序，但不会把日志或状态写入系统目录。需要回退时，
请从项目的 [Releases](../../releases) 页面下载对应的历史版本。

## 首次测试

1. 确认 Wi-Fi 已连接。
2. 启动后等待热门目录出现。
3. 选择一个条目，进入搜索结果后选择匹配番剧。
4. 选择一集播放；默认请求适合 640×480 屏幕的 480p 视频。
5. 按 B 返回，再到“历史”确认剧集与进度已记录。

## 日志位置

Windows 中通常位于 ROMS 分区：

`MUOS/application/KazumiLite/data`

出错时提供 `log.txt` 和 `mpv.log`。搜索或剧集加载失败时，先检查 Wi-Fi 和设置中的“运行环境检查”。
